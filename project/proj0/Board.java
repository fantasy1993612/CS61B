public class Board
	

	/*Board(boolean shouldBeEmpty) - The constructor for Board.
 	If shouldBeEmpty is true, initializes an empty Board. Otherwise, 
 	initializes a Board with the default configuration. 
	Note that an empty Board could possibly be useful for testing purposes.*/
	public Board(boolean shouldBeEmpty){
		if(shouldBeEmpty)
	}
	/*Piece pieceAt(int x, int y) - Gets the piece at position (x, y) on the board, 
	or null if there is no piece. If (x, y) are out of bounds, returns null.*/
	Piece pieceAt(int x,int y){

	}
	public static void main(String[] args){

	}
}